# Database

The most recent database schema for this application is stored in `schema.sql`.

The migrations are stored in `migrations/` and will be applied by the application when the `migrate` command is run.