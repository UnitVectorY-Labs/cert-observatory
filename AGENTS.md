# Documentation Conventions

Documentation follows Jekyll conventions for GitHub Pages. All markdown files in `docs/` should have YAML front matter specifying layout, title, and nav_order.

The `_config.yml` file configures the Just the Docs theme for the documentation site.
